/**
 * @file Prerequisites.h
 * @brief Contiene las inclusiones de librerías estándar de C++ más utilizadas en el proyecto.
 *
 * Este archivo actúa como una cabecera de precompilación simple, permitiendo incluir
 * todas las dependencias comunes en un solo lugar y manteniendo los demás headers limpios.
 */
#pragma once

 /// Incluye iostream para operaciones de entrada y salida de datos (ej. std::cout, std::cin).
#include <iostream>
/// Incluye string para manejar la clase std::string (cadenas de texto).
#include <string>
/// Incluye vector para manejar el contenedor dinámico std::vector (almacenamiento de tickets).
#include <vector>
/// Incluye ctime para funciones relacionadas con el tiempo y las fechas (ej. time, localtime).
#include <ctime>
/// Incluye algorithm para usar funciones de utilidad como std::transform y std::remove_if.
#include <algorithm>